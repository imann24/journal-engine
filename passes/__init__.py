"""Derivation passes for the journal signal store.

`deterministic.py` holds the source-of-truth contracts (Chunk, SignalRow, Pass,
SignalStore, PassRunner) and the two deterministic passes (GoEmotions, Lexical).
`llm_profile.py` is a stub extension point for a future LLM pass.
"""
